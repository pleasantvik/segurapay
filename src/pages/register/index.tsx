// import { RegisterForm } from "@/components/auth/registerForm";
import { RegisterForm } from "@/components/auth/RegisterForm";
import React from "react";

const Register = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Register;
